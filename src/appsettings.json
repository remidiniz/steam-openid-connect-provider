{
  "AllowedHosts": "*",
  "OpenID": {
    "ClientID": "${OPENID_CLIENT_ID}",
    "ClientSecret": "${OPENID_CLIENT_SECRET}",
    "ClientName": "${OPENID_CLIENT_NAME}",
    "RedirectUri": "${OPENID_REDIRECT_URI}",
    "PostLogoutRedirectUri": "${OPENID_LOGOUT_URI}",
    "IssuerUri":  "${OPENID_ISSUER_URI}"
  },
  "Hosting": {
    "BasePath": "${HOSTING_BASE_PATH}"
  },
  "Steam": {
    "ApplicationKey": "${STEAM_API_KEY}"
  }
}